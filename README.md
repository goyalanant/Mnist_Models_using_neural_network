## Mnist_Models_using_neural_network

THere are two csv file where the accuracy and losses of training and testing data after every epoch are saved.

## There is not that sigficant differnce on testing loss and accuracy using dropout layer and without using it.
    
